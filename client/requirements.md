## Packages
framer-motion | Complex animations, page transitions, and cursor effects
clsx | Conditional class names
tailwind-merge | Merging tailwind classes safely

## Notes
Voice Agent uses Web Speech API (native browser support)
Chat Widget connects to POST /api/chat
Dynamic cursor effect requires disabling default cursor in some areas
Dark mode is default
